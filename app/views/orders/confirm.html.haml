- @title = t("order.confirm")
#content
  %h1 #{@title}

  %h2 #{@order.rubyist.username}'s order:

  %ul
    - @order.line_items.each do |item|
      %li
        #{t(item.product_item.label)},
        #{number_with_delimiter(item.quantity)},
        #{number_with_delimiter(item.subtotal_price)}
        - if item.individual_sponsor?
          <br />
          Individual Sponsor Link Label: <strong>#{item.link_label}</strong>
          <br />
          Individual Sponsor Link URL: <strong>#{item.link_url}</strong>
          <br />
          - if ProductItem.kaigi(2010).rk10_party.now_on_sale?
            I <strong>#{ item.attend_party? ? "will " : "will not" }</strong> attend offician conference party on 28th Aug.
          - else
            (#{t('individual_sponsor_party_attend_bonus_is_over')})

  - if @order.individual_sponsor_included?
    != link_to 'Edit Individual Sponsor Options', individual_sponsor_option_orders_path
    <br /><br /><br /><br />
  - form_tag(url_of_paypal_checkout) do
    != hidden_field_tag :cmd, @paypal_form.cmd
    != hidden_field_tag :encrypted, @paypal_form.encrypted
    != submit_tag 'Place your order via PayPal'