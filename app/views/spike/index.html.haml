.menu
  %h2 mayuco design
  %ul
    %li!= link_to 'New Top Page 2010(ja)', '/2010/ja'
    %li!= link_to 'New Program Page 2010(ja)', '/2010/ja/programs'

.latest_news
  %h2 Latest News

  %dl
    - @headlines.each do |h|
      %dt= l h.time.to_date, :format => :long
      %dd!= link_to h(h.title), h.link

%h2 yeah! why don't you join us?
!= link_to 'registrations', registrations_path
<br />
!= link_to 'cart', carts_path
<br />

%h2 Individual Sponsors
- sponsors = Contribution.individual_sponsors_of(2010)
- unless sponsors.empty?
  %ul
    - sponsors.each do |sponsor|
      %li!= link_to h("#{sponsor.link_label}(#{sponsor.amount}JPY)"), sponsor.link_url
- else
  (Not Yet)
